'use client';
import React, { useState } from 'react';
import { twMerge } from 'tailwind-merge';

const NavProfileMenu = () => {
	return (
		<div>
			<div className='ml-4 rounded-full bg-lime-300 w-12 h-12 text-black flex items-center justify-center font-bold text-xl hover:bg-lime-400'>
				A
			</div>
		</div>
	);
};

export default NavProfileMenu;
